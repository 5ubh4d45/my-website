export const DEFAULT_THEME = 'night';
export const DARK_MODE = true;

export const THEME_LIST = {
    light: [
        "pastel",
        "light",
        "winter"
    ],
    dark: [
        "night",
        "dark",
        "forest"
    ]
}

export const DEFAULT_THEME_DATA = {
    light: THEME_LIST.light[2],
    dark: THEME_LIST.dark[0]
}